# Статус проекта

> Живая память проекта: обновляется в конце каждой значимой сессии (правило — в CLAUDE.md).
> История изменений — в git; здесь только актуальное состояние. Держать до ~40 строк.

**Обновлено:** 2026-07-19 (четвёртая сессия)

## Где мы

- **Фаза:** этап 1 завершён (30/30); **этап 2 расширен с санузла до этажа**: по чистой
  АР-модели (Snowdon Architectural, без MEP) спроектирован ВК всего L1 — 6 кластеров,
  185 труб + 90 отводов в Revit, лист ВК-1 (план + спецификация 215,9 м + марки) —
  эксперимент [03](../experiments/03-vk-etazh-l1.md).
- **Ключевая новация:** точки подключения синтезируются по нормам (СП 73 + табл. А.1) —
  calc/trace/connectors.py; решение в docs/decisions.md.
- **Чек-лист v1:** +3 проверки (отвод ≥ выпуска, высоты СП 73, Hтр ф. 14); 193 проверки
  зелёные; 98 тестов. **Сабагент-нормоконтролёр опробован в бою:** нашёл 4 нарушения вне
  зоны чек-листа (узел стояк→коллектор 90°, нет вытяжных частей, мойки без разрыва струи,
  Ду110 в тонких стенах) — experiments/03-vk-etazh-l1/normokontrol.md.
- **Clash detection:** 200 пар (167 стены — систематика «прокладка по оси стены», 33
  перекрытия) — clash_report.json; связка «чек-лист для чисел + сабагент для требований +
  clash для геометрии» работает.
- **Модель:** сэмпл read-only → рабочая копия `models/Snowdon_Towers_AR_VK_L1.rvt`
  (вне git); id созданного — created_ids.json. Свежие грабли Revit MCP (кириллица
  latin-1, Regenerate для фитингов, свежие коннекторы) — docs/lessons.md.

## Следующие шаги

- [ ] Генератор узлов v1 по замечаниям нормоконтроля: стояк→коллектор из 2×45°
      (п. 18.4/18.5), разрыв струи моек общепита (п. 18.12), вытяжные части/клапаны
      стояков (п. 18.18/18.22) с пересчётом по К.8, тройники и редукторы в узлах
- [ ] Трассировка v1: точки подключения к грани стены (не оси), учёт всех стен кластера
      (двойные конструкции), проходки/гильзы, функционал нижних помещений (п. 18.11)
- [ ] Вопросы валидации: переформулировать 11–12, оцифровка заготовок 31–41; табл. А.2
      (U, q_hr_u) — убрать демо-значения из паспортов
- [ ] Русские шаблоны оформления: метрический каталог труб, марки Ду вместо дюймов
- [ ] Renga: дистрибутива на машине нет — нужен дистрибутив или другая машина

## Заметки для следующей сессии

- Тесты: `uv run pytest` (98). Пайплайн этажа: build_passport.py → run_pipeline.py →
  make_revit_payload.py → создание через execute_revit_code (см. эксперимент 03).
- Кириллица в execute_revit_code/MCP-параметрах — мохибейк: чинить
  `s.encode('latin-1').decode('utf-8')`, искать элементы по id (docs/lessons.md!).
- Лист ВК-1: id вида 2663303, листа 2663372, спецификации 2663365 (в рабочей копии).
- PDF СП: C:\SNIPpars\downloads_faufcc\add; карта PDF СП 30 — в памяти/зеркале.
